const client = new (require('discord.js').Client)({ intents: 32767 })
const token = ""
const ownerID = ""
const GuildId = ""
const prefix = ""

module.exports = {
    client, token, ownerID, GuildId, prefix
}